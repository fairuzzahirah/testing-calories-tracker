# Calories Tracker Test Configuration

# Application URLs
base.url=http://localhost:8080
backend.url=http://localhost:8000

# Browser Configuration
browser=chrome
headless=false

# Timeouts (in seconds)
timeout=10
page.load.timeout=30

# Test Data
demo.email=demo@example.com
demo.password=password123

# Test Environment
environment=local

# Reporting
generate.reports=true
screenshot.on.failure=true

# API Configuration
api.timeout=30
api.base.url=http://localhost:8000/api
api.key=your_usda_api_key_here
gemini.api.key=your_gemini_api_key_here

# Database Configuration (if needed for test data setup)
db.host=localhost
db.port=3306
db.name=calories_tracker
db.username=root
db.password=

# Test Execution
parallel.execution=false
max.retry.count=2
test.data.cleanup=true
db.host=localhost
db.port=3306
db.name=calories_tracker
db.username=root
db.password=
